const happyDay = ['23', '42', '44', '444'];

happyDay.filter((item) => {
  console.log(item);
});

const uglyDat = ['23', '333', '23333', '22222'];

uglyDat.filter((item) => {
  console.log(item);
});

const hopeItsAGoodDay = ['22222', '23'];

for (let i = 0, len = uglyDat.length; i < len; i++) {
  console.log(i);

  const anObjectIsNotEnoughForMeToSatisfy = {
    moreObject: 'moreObject?',
  };
}
